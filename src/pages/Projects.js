import { Typography, Box } from "@mui/material"

export default function Projects(){
    return(
        <Box sx={{maxWidth:"100vw"}}>
            <Typography sx={{paddingTop:7}} variant="h2">Projects</Typography>
        </Box>
    )
}